import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container:{
    marginBottom: 20
  },
  label: {
    fontSize: 16,
    color: "#fff",
    marginBottom: 8
  },
  input: {
    padding: 4,
    borderColor: "#fff",
    borderWidth: 1,
    borderRadius: 10,
    paddingHorizontal: 8
  }
});

export default styles;